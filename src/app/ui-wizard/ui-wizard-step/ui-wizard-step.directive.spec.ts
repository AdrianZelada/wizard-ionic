import { UiWizardStepDirective } from './ui-wizard-step.directive';

describe('UiWizardStepDirective', () => {
  it('should create an instance', () => {
    const directive = new UiWizardStepDirective();
    expect(directive).toBeTruthy();
  });
});
